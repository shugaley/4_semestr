
#include "Integration.h"

#include "stdio.h"

// ==== API ====================================================================

int Integrate(size_t nThreads, size_t* res) {

    if (res == NULL)
        return INTEGRATION_EINVAL;

    return 0;
}